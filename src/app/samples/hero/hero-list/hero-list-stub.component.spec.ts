import { Component, OnInit } from '@angular/core';
import { Hero } from '../hero.types';
import { HeroService } from '../hero.service';

@Component({
  selector: 'app-hero-list',
  template: ''
})
export class HeroListStubComponent {
}
